---
title: CLI Walkthrough
description: Install and use the @ckb-firewall/cli package for registry inspection and governance flows.
---

import cliScreenshot from '../../../../../assets/cli-screenshot.png';
import { Steps } from '@astrojs/starlight/components';

`@ckb-firewall/cli` is the validator and viewer tool for inspecting the blacklist registry, reviewing proposals, voting, and building governance transactions after validator approval.

Published on npm as [@ckb-firewall/cli](https://www.npmjs.com/package/@ckb-firewall/cli).

## Install

```bash
npm install -g @ckb-firewall/cli
```

Or use the one-line installer:

```bash
curl -fsSL https://raw.githubusercontent.com/digitaldrreamer/ckb-transaction-firewall/main/scripts/install-cli.sh | bash
```

## Inspect the live registry

```bash
ckb-firewall inspect
```

Reads the canonical testnet registry cell and prints all entries with their status (`active` or `EXPIRED`) and expiry dates. For treasury-backed registries it also prints registry capacity use, treasury balance, blacklist pool usage, and the treasury donation address. If pool usage is 70% or higher, `inspect` shows a donation warning.

<figure>
  <img src={cliScreenshot.src} alt="Screenshot of the ckb-firewall CLI" />
</figure>

## Check a specific address

```bash
ckb-firewall check --lock-args 0xabc123...
```

Returns immediately with whether the address is currently blacklisted.

## Governance flow

All registry changes go through governance. The proposal lives as a **local JSON file** until `execute` writes it on-chain.

<Steps>
1. **Propose** — create a proposal locally and start the 72-hour review window
2. **Anchor** — create the on-chain `PBLK` proposal cell
3. **Export** — share the proposal JSON with other participants
4. **Import** — each participant receives and merges the proposal
5. **Vote** — each validator signs their vote with their private key
6. **Execute** — build and submit the on-chain registry update transaction after the review window and vote threshold
</Steps>

```bash
# Create and share a proposal
ckb-firewall propose
ckb-firewall anchor --proposal <id> --to-address <proposal-cell-owner-address>
ckb-firewall anchor --proposal <id> --proposal-tx <anchor-tx> --proposal-index <data-output-index>
ckb-firewall export --proposal <id> --out proposal.json

# Other participants import, vote, re-export
ckb-firewall import proposal.json
ckb-firewall vote --proposal <id> --vote yes
ckb-firewall export --proposal <id> --out proposal-voted.json

# After 72h review window + 3/5 yes votes: execute on-chain
ckb-firewall execute --proposal <id>
```

For a complete walkthrough with explanations of each step, see [Blacklisting an Address](/guides/governance-blacklist/) and the [Governance Runbook](/operations/governance-runbook/).

## Track proposal status

```bash
ckb-firewall proposals
ckb-firewall proposals --status voting
ckb-firewall proposals --status approved
```

## Full reference

[CLI Reference](/reference/cli/)
