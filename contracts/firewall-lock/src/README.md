# Firewall Lock Source

This source folder will contain the lock script implementation for:

- reading the blacklist registry `cell_dep`,
- matching output `lock_args` / `type_args`,
- rejecting invalid transactions by returning script errors.
