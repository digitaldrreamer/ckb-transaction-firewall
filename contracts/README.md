# Contracts

This directory contains the on-chain CKB scripts that enforce firewall behavior.

## Planned submodules

- `firewall-lock/`: lock script that enforces blacklist checks during spend validation.
- `blacklist-registry/`: type script that protects governance-controlled blacklist updates.
